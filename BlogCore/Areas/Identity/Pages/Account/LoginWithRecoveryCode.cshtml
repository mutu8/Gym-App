@page
@model LoginWithRecoveryCodeModel
@{
    ViewData["Title"] = "Verificación con código de recuperación";
}

<header class="text-center mt-5">
    <h1 class="display-4 text-info">@ViewData["Title"]</h1>
    <hr />
</header>

<div class="text-center">
    <p class="lead text-muted">
        Has solicitado iniciar sesión con un código de recuperación. Este inicio de sesión no será recordado hasta que ingreses
        un código de la aplicación de autenticación al iniciar sesión o desactives la autenticación en dos pasos (2FA) y vuelvas a iniciar sesión.
    </p>
</div>

<div class="row justify-content-center">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>
            <div class="form-floating mb-3">
                <input asp-for="Input.RecoveryCode" class="form-control" autocomplete="off" placeholder="Código de recuperación" />
                <label asp-for="Input.RecoveryCode" class="form-label">Código de recuperación</label>
                <span asp-validation-for="Input.RecoveryCode" class="text-danger"></span>
            </div>
            <div>
                <button type="submit" class="w-100 btn btn-lg btn-primary">Iniciar sesión</button>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
